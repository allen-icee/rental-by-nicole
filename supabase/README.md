# Supabase

Keep database, storage, and security work here.

- `migrations/`: PostgreSQL schema, indexes, triggers, and RLS policies.
- `seed/`: development seed data.
- `storage/`: bucket notes and storage policy references.

The app should use RLS for all customer and admin data access.
